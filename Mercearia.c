
switch (codigoBarras) {
        case 1:
            printf("Leite Integral - 3,80");
            break;
        case 2:
            printf("Manteiga - 5,98");
            break;
        case 3:
            printf("Café 500g - 8,25");
            break;
        default:
            printf("Produto não cadastrado");
            break;

    }


    //Informe seu código aqui
        float nota =0.0;
        nota = ((p1 + (p2 * 2) + (p3 * 3)  + tf)) / 7

        if (nota >= 9) {
            printf("A");
        } else {
            if (nota >= 7.5) && (nota < 9.0) {
                printf("B");
            } else {
                if (nota >= 6.0)  && (nota <7.5) {            {
                    printf("C");
               }  else {
                    printf("D");
                }
              }
            }
